"""D4: methodology assets summary + final handover refresh."""
import json
import os

ROOT = r'D:\deepseek_harness\prp49'

doc = r"""# PRP49 · 方法学资产汇总

> 2026-09-18 · 本文件汇总项目的**可复用方法学产出**：验证协议阶梯、回归评估规范、
> 四条路线的定量边界、以及 44 条踩坑记录中最重要的部分。
> 详细数据见 `docs/PRP49_推进总览.md`、`docs/PRP49_版本对齐.md`、`docs/PRP49_ACD路线计划.md`。

---

## 一、CV 协议阶梯（四档严格性递进）

| 档 | 协议 | 问题设定 | AUC | 折间 std | 用途 |
|---|---|---|---|---|---|
| 0 | 分层 stratified | 同肽可跨折 | 0.8145 | 0.0194 | ⚠️ **有泄漏，仅作历史参考** |
| 1 | **按肽分组** grouped | 新肽 × 已知靶 | **0.8333** | **0.0135** | **主判据** |
| 2 | **按家族分组** family | 新家族 × 已知靶 | **0.8142** | 0.0477 | 稳健性 / **证伪家族记忆** |
| 3 | 按靶分组 by-target | 已知肽 × **新靶** | 0.6430 | 0.0226 | 跨靶零样本 |
| 4 | 跨物种域外 | 普通肽 × 蛋白 | 0.470 → **0.6421**（多任务）| 0.0335 | 域外泛化 |

**三条读数**：
1. **从"新肽"到"新家族"仅降 0.019** → 模型学到的是**跨家族泛化规律**，不是家族记忆
2. **family 协议的 std 是 grouped 的 3.5 倍**（0.0477 vs 0.0135）→ 更严但更嘈杂，
   宜作**证伪工具**而非打分尺子（路线 A 正是在此暴露）
3. **多任务把跨域从 0.470 提到 0.6421**（+0.172），代价是域内 −0.090

---

## 二、回归评估规范（RMSE 约束方案）

### 2.1 必须同框报告的三条线

| 线 | 定义 | 方案 A | 方案 B |
|---|---|---|---|
| **mean-baseline** | 用**训练折均值**预测（无信息基线）| 1.813 | 1.623 |
| **模型** | tanh 约束 + base_head | **1.530 ± 0.067** | **1.472** |
| oracle 参考 | 测试折标签的**自身离散度** | std ~1.5–2.3 | — |

> **只有同时给出前两条**，才能区分"模型学到了信号"与"只是预测到了均值附近"。
> 早期版本因缺少这一对照，把**偏移**误判成了**学习**（RMSE 6.855 的发散就是这么被发现的）。

### 2.2 结论

| 项 | 结果 |
|---|---|
| tanh 输出约束 | ✅ **彻底解决发散**（6.855 → 1.5）|
| base_head 均值–残差分解 | ✅ 可测增益（方案 A 1.530 vs 无 base_head 的 1.725）|
| Pearson（方案 A）| **0.546**（说明学到了**排序**信号，不只是偏移）|
| 相对改善 | A **+15.6%** / B **+9.3%** |
| **实用门槛** | ❌ **未达**（要求 RMSE < 1.0）|

**两者相差仅 0.058，在 5 折上难以判定谁显著更优** —— 诚实结论：
**约束方法成功，但该数据规模下回归尚不可用**。

---

## 三、四条探索路线的定量边界

| 路线 | 假设 | 定量结果 | 判定 |
|---|---|---|---|
| **A** 家族软标签传播 | 同家族 ≈ 同靶，可扩正样本 | family CV **0.7481 ± 0.0506**（基线 0.8142）| ❌ **判负** —— 传播引入的噪声大于信号 |
| **C** 家族对比预训练 | 无标签数据可提升表征 | 预训练分离度 0.024→0.442；下游 5 折 **0.8248 vs 0.8333**，**5/5 折为负**，配对 t = −2.66 | ❌ **判负** |
| **D′** 域同源加权辅助 | 同源靶可提供额外监督 | 域内 **+0.061** ✅ / 跨域 **−0.072** ❌ | ⚖️ **权衡**（非改进）|
| **D v2** 加真实复合物 | 真实复合物应更有效 | 域内 **0.7915**（低于 D′ 的 0.8037）| ❌ **无增益** |
| **MTL** 多任务跨域 | 共享主干可迁移 | 跨域 **0.470 → 0.6421** | ✅ **唯一明确正面成果** |

### 3.1 最有价值的一条方法学教训

> **自监督指标与下游性能可以完全脱钩。**
> 对比预训练把负对相似度从 0.966 压到 0.502、分离度提升 18 倍，
> 下游 5 折却**一致地更差**。若只看预训练指标就会误判为成功。

**机制**（可解释，非玄学）：
1. 训练数据高度冗余（4,749 条中最大簇 **1,109 条**，簇内 identity 0.95–1.00）
   → 对比学习学到的是**冗余记忆**
2. 下游仅解冻 1 层（`lassoesm_unfreeze: 1`）→ 预训练表征**几乎被绕过**
3. 任务错配：预训练目标是"家族判别"，下游是"肽–靶结合"，**共享信息有限**

---

## 四、验证纪律（从 44 条坑中提炼）

### 4.1 最危险的一类：**不报错但给出错误结果**

| # | 坑 | 后果 |
|---|---|---|
| 38 | 并行任务共享 checkpoint 目录 | **"2 分钟假完成"**：静默评估别人的权重并输出看似正常的 RMSE |
| 39 | 单一关键词检索 | 漏掉 6N60/61/62，会让人误判"数据不存在" |
| 40 | 多亚基复合物取第一个匹配 | 靶链选成 omega（91 aa）而非 β′（1409 aa） |
| 41 | 只做语法检查 | 作用域错误让 5 个作业秒级全灭，而 `ast.parse` 完全通过 |
| 42 | 核酸链按长度当作肽 | DNA 序列混入"lasso 肽"数据集 |
| 43 | 对比基线协议不匹配 | 把"5/5 折为负"读成"略优"（结论方向相反）|
| 44 | 假设时间而不实测 | 闹钟设到已过去的时刻，静默失效 |
| 26 | `sum(1 for x,y in zip())` 漏 `if x==y` | 相似度恒为 1.0，软标签全部虚高 |
| 17/18 | `line[:6]=='ATOM'` 永假 / 空文件欺骗进度 | 39 个受体成空文件；429 个任务"完成"实为 0 字节 |

### 4.2 由此形成的检查阶梯

```
语法检查 (ast.parse)
   ↓  不足以发现问题 41
作用域/语义检查（AST 遍历自由变量、调用点签名）
   ↓
阳性对照（如"靶序列能否命中自身结构域" 11/11）
   ↓  没有它会把"API 静默返回空"误判为"数据不存在"
冒烟测试（小样本真跑一遍）
   ↓
提交前 preflight（换行符/表头/时限可行性/多配置拆分）
   ↓
运行时守卫（完成态 checkpoint 拒绝静默评估；协议随指标一起记录）
```

### 4.3 数据侧的三条铁律

1. **序列 ≠ 蛋白质**：按**沉积的 entity 类型**过滤，不按长度（#42）
2. **多链复合物禁用 `iloc[0]`**：用区分性字段 + 最长匹配（#40）
3. **协议随数字一起记录**：引用任何 AUC 必须附协议名（#43）

---

## 五、可复用产物

| 产物 | 位置 | 用途 |
|---|---|---|
| 验证协议阶梯 | `config_*_grouped/family.yaml` | 任何新模型都应在两档下报告 |
| RMSE 约束训练器 | `prp49/train_reg_bound.py` | tanh 边界 + base_head + 基线同框报告 |
| 断点续训 | `prp49/ckpt.py` | 原子写入 + 完成态守卫 + 轻量化（3.03 GB → 219 MB）|
| 并行折工具 | `--fold` + `--ckpt_dir` + `merge_reg_folds.py` | 串行 35h → 并行 7h |
| 靶标结构数据库 | `lasso_target_db/` | 6 个真实复合物 + 人靶↔对应物映射 |
| 结构优化流水线 | `mut_variants` / `ring_variants` / `tail_modification_advice` | 饱和突变 + 环大小 + 尾部修饰 |
| 阳性对照脚本 | `domain_positive_control.py` | 任何同源/域搜索前先自检 |
"""
p = os.path.join(ROOT, 'docs', 'PRP49_方法学资产汇总.md')
open(p, 'w', encoding='utf-8').write(doc)
print(f'wrote {p} ({len(doc.splitlines())} lines)')

import shutil
for rel in ['v2_2026-09-18_full', 'FINAL_2026-09-18_v2_full']:
    shutil.copy2(p, os.path.join(ROOT, 'releases', rel, 'PRP49_方法学资产汇总.md'))
print('copied into both releases')
